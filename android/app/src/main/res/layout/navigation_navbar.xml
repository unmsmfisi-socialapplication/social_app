<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="bottom"
        android:orientation="horizontal"
        android:paddingTop="10dp"
        android:paddingBottom="20dp"
        android:weightSum="5"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/icon_home"
            style="@style/ImageIconNavBar"
            android:contentDescription="@string/icon"
            app:srcCompat="@drawable/navbar_icon_home" />

        <ImageView
            android:id="@+id/icon_add_user"
            style="@style/ImageIconNavBar"
            android:contentDescription="@string/icon"
            app:srcCompat="@drawable/navbar_icon_adduser" />

        <ImageView
            android:id="@+id/icon_business"
            style="@style/ImageIconNavBar"
            android:contentDescription="@string/icon"
            app:srcCompat="@drawable/navbar_icon_business" />

        <ImageView
            android:id="@+id/icon_friends"
            style="@style/ImageIconNavBar"
            android:contentDescription="@string/icon"
            app:srcCompat="@drawable/navbar_icon_friends" />

        <ImageView
            android:id="@+id/icon_profile"
            style="@style/ImageIconNavBar"
            android:contentDescription="@string/icon"
            app:srcCompat="@drawable/navbar_icon_user" />
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>